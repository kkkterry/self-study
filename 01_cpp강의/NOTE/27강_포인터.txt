27강. 포인터
주소를 가리키는 변수
자료형 + * 변수명
해당 포인터에게 전달된 주소를 해석하는 단위

int i = 100;
float f = 3.14;
int* pInt = &i; //인트 자료형의 변수를 가리키는 변수
int* pInt = (int*)&f; // 강제 케스팅

// 주소로 접근
//(*pInt) = 100;	// i라는 변수에 100입력
i = *pInt; 		// 값이 이상한 값이 나온다. float을 int로 읽어서 생기는 문제

// 주소의 단위
// BYTE